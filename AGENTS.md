# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

This project is on **SDK 54** (`expo ~54.0.0`, React Native 0.81.5). Bump this link when the
SDK is upgraded — reading the wrong version's docs is how environments get misconfigured.
